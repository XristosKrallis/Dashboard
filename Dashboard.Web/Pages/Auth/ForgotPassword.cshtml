@page
@model Dashboard.Web.Pages.Auth.ForgotPasswordModel
@{
    Layout = "_AuthLayout";
    ViewData["Title"] = "Forgot Password";
}

<div class="container-scroller">
    <div class="container-fluid page-body-wrapper full-page-wrapper">
        <div class="content-wrapper d-flex align-items-center auth px-0">
            <div class="row w-100 mx-0">
                <div class="col-lg-4 mx-auto">
                    <div class="auth-form-light text-left py-5 px-4 px-sm-5">
                        <div class="brand-logo">
                            <img src="~/images/logo.svg" alt="logo" />
                        </div>
                        <h4>Forgot your password?</h4>
                        <h6 class="font-weight-light">Enter your email to reset it.</h6>

                        <form method="post" class="pt-3">
                            <div class="form-group">
                                <input type="email" asp-for="Email" class="form-control form-control-lg" placeholder="Email" />
                                <span class="text-danger">@Html.ValidationMessageFor(m => m.Email)</span>
                            </div>

                            <div class="mt-3 d-grid gap-2">
                                <button type="submit" class="btn btn-block btn-primary btn-lg font-weight-medium auth-form-btn">
                                    SEND RESET LINK
                                </button>
                            </div>

                            <div class="text-center mt-4 font-weight-light">
                                Remembered your password?
                                <a asp-page="/Auth/Login" class="text-primary">Login</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        <!-- content-wrapper ends -->
    </div>
    <!-- page-body-wrapper ends -->
</div>
